# Salai CLI for Claude Code

Follow `../agent-spec-short.md`.

- Execute `salai` via shell commands.
- Prefer `--json` and parse stdout as JSON.
- If store is missing, set it and retry.
- Never print `SALAI_API_KEY`.
