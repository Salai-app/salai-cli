# Salai CLI for Codex-style Agents

Follow `../agent-spec-short.md`.

- Use `salai ... --json` for machine output.
- Parse results and extract `itemCode` when searching.
- Recover from `SELECTED_STORE_REQUIRED` by setting store.
- Keep responses concise and do not expose secrets.
