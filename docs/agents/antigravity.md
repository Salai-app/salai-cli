# Salai CLI for Antigravity Agents

Follow `../agent-spec-short.md`.

- Prefer JSON-mode calls with `--json`.
- Use deterministic flow: store -> search -> itemCode -> cart and compare.
- Handle `SELECTED_STORE_REQUIRED` by setting a store first.
- Never include secrets in output.
