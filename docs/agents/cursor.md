# Salai CLI for Cursor Agents

Follow `../agent-spec-short.md`.

- Use `--json` unless the user explicitly asks for formatted output.
- Apply store setup flow before store-scoped commands.
- Parse JSON defensively and avoid exposing credentials.
